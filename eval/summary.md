Evaluation summary

Baseline parse success rate: <PLACEHOLDER — compute after running evaluations>

Instructions:
- Run base model on 20 held-out docs and fill `eval/baseline_responses.md` and `eval/baseline_scores.csv`.
- Compute parse success rate as: number of rows where `is_valid_json`==True AND `has_all_required_keys`==True divided by 20.
- Enter the computed percentage in the line above.
